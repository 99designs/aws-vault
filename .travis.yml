language: go

go:
  - "1.12.x"

os:
  - linux
  - osx

env:
  - GO111MODULE=on CGO_ENABLED=0

script:
  - diff -u <(echo -n) <(gofmt -d $(git ls-files '*.go'))
  - go vet ./...
  - CGO_ENABLED=1 go test -race ./...
